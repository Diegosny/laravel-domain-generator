name: Setup PHP

runs:
  using: composite

  steps:
    - uses: shivammathur/setup-php@v2
      with:
        php-version: '8.4'
        coverage: none

    - uses: ramsey/composer-install@v3
      with:
        dependency-versions: locked